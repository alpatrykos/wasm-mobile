[workspace]
members = ["shared-core"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
panic = "abort"
strip = "symbols"

